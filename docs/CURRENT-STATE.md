# Current State

Canonical project status for `silverman-blog-linkedin`. Authority rules: [CONTEXT-AUTHORITY.md](CONTEXT-AUTHORITY.md). Terminology: [GLOSSARY.md](GLOSSARY.md). Live flags: [RUNTIME-STATE.md](RUNTIME-STATE.md).

**`last_verified_at_utc`:** `2026-07-17T20:46:19Z`
**Last verified baseline revision:** `da21e99` (worker content serving BL-005 window — **not** a permanent runtime requirement)

## Purpose

Local HTTP worker for blog-to-LinkedIn content automation. n8n orchestrates over HTTP only (ADR-0001). The worker owns filesystem boundaries, validation, generation, metadata, and editorial lifecycle moves.

## Business goals

- **Short-term:** Attract recruiters and C-level executives for remote senior roles (~USD 7k/month).
- **Long-term:** Recognition in AI, architecture, digital transformation, agility, governance, and technology efficiency.

## Architecture summary

```
n8n (orchestrator, HTTP only) → Worker (FastAPI) → Editorial dirs + public blog checkout
                                      ↓
                              Postgres `silverman_linkedin_db` (master editorial calendar SoT; ADR-0004)
                                      ↓
                              ComfyUI (optional images), DeepSeek (LLM)
```

- **47** canonical OpenSpec specs (includes `editorial-backup-restore-recovery` after US-037 sync) (strict validation passing)
- **1,157** automated tests collected after US-035 restart validation (10 focused US-035 tests + prior suite; US-033/US-034/BL-012/BL-008 regression slice 153 passed)
- Worker deployed at `http://192.168.0.194:8010` (see [RUNTIME-STATE.md](RUNTIME-STATE.md))
- Editorial base: `/data/silverman-blog-linkedin` (container); public GitHub Pages checkout: `/public-blog`

Key endpoints include `GET /health`, read-only `GET /flow-a/operational-status`, authenticated Flow A incomplete-campaign recovery (`GET /flow-a/incomplete-campaign-recovery/{campaign_id}`, `POST .../resume`, `POST .../repair`, `POST .../cancel` — US-031 + US-032, deployed/accepted), authenticated `POST /flow-a/operational-alerts/evaluate` (US-028 + US-029 + US-030) and `POST /flow-a/operational-alerts/report-orchestration-failure` (US-030), Flow A (`POST /publish-blog-post`, `/generate-linkedin-package`, `/schedule-linkedin-distribution`, `/complete-flow-a-ready-path`, calendar connector), LinkedIn publication (`/queue-linkedin-publication`, `/publish-linkedin-due-variants`, `/cancel-linkedin-publication`), Flow B (`GET /flow-b/calendar-gaps`, `GET`/`PUT /flow-b/gap-operator-settings`, `POST /flow-b/gap-trigger` — US-082 implemented and deployed; `POST /flow-b/discover-topics`, `POST /flow-b/generate-blog-drafts` — US-079 implemented and deployed; `GET`/`POST /flow-b/pending-approval-drafts…` — US-080 implemented and deployed; `POST …/promote` — US-081 implemented and deployed), editorial content backlog (`GET`/`POST /editorial/content-backlog`, `GET`/`PUT /editorial/content-backlog/{item_id}`, `PUT /editorial/content-backlog/reorder` — US-049 + US-050 **implemented locally, not deployed / not Story accepted**), and Flow B-adjacent draft endpoints (`/process-ready`, `/process-file`, `/generate-linkedin-draft`).

## Ownership matrix

| Concern | Owner |
|---------|-------|
| Editorial source approval | Human operator |
| Flow A validation and lifecycle | Worker |
| Image generation | Worker → ComfyUI |
| Public checkout file writes | Worker (handoff only) |
| Git commit / push (guarded) | Worker when enabled **and** request opts in (`git_publication: true`); manual fallback documented |
| Live-site HTTP confirmation (guarded) | Worker when enabled **and** request opts in (`live_site_confirmation: true`) after Git push evidence |
| LinkedIn package generation | Worker |
| LinkedIn schedule metadata | Worker |
| LinkedIn real API publish | Worker when explicitly enabled — **operationally validated** (US-003; guard `false` by default) |
| Workflow timing / orchestration | n8n Schedule Trigger daily 09:00 UTC (server active; repo export inactive) |
| Secrets and environment flags | Operator |
| Deployment | Operator (`deploy/server/deploy-worker.sh`) |
| Behavioral requirements | Canonical OpenSpec specs |
| Real behavior evidence | Implementation and tests |
| Current status and known divergences | This document |
| Volatile live flags | [RUNTIME-STATE.md](RUNTIME-STATE.md) |
| Editorial policy | `content-strategy/silverman-editorial-system.md` |

## Runtime topology

| Item | Value |
|------|-------|
| Server | `192.168.0.194` |
| Worker port | `8010` |
| Editorial host path | `/home/silverman/compartido_mac/silverman-blog-linkedin` |
| Public blog host path | `/home/silverman/silverberdi.github.io` |
| Deploy guide | [ubuntu-server-worker-deployment.md](deployment/ubuntu-server-worker-deployment.md) |

## Operationally validated

Evidence from real post `04-a-bounded-context-is-not-a-folder.md` (2026-07-10):

- Flow A core end-to-end: ComfyUI image, validation, blog handoff, package, schedule, lifecycle, campaign **`flow_a_complete`**
- Blog live at `https://silverman.pro/2026/07/10/a-bounded-context-is-not-a-folder/` after **manual** Git commit/push
- `POST /publish-blog-post` idempotency: `already_published` with no metadata side effects
## Operationally validated (recent)

- Guarded Git commit/push after blog handoff with per-request `git_publication: true` (US-001) — operationally validated on `192.168.0.194` with real remote push to `origin/main`; smoke artifacts removed from public site and editorial mount; see [phase3-us001-git-publication-validation-2026-07-11.md](operations/phase3-us001-git-publication-validation-2026-07-11.md)
- Live-site HTTP confirmation after Git push with per-request `live_site_confirmation: true` (US-002) — operationally validated on `192.168.0.194` (`blog_live_site_publication.status: confirmed`, HTTP 200 + slug marker during validation window); smoke artifacts removed; see [phase3-us002-live-site-confirmation-validation-2026-07-11.md](operations/phase3-us002-live-site-confirmation-validation-2026-07-11.md)
- LinkedIn API real publish (US-003/US-004/US-005, BL-002) — operationally validated on `192.168.0.194`: OAuth + queue + publish-due with `publish_now`, `linkedin_post_urn` stored, operator-confirmed visibility, idempotent rerun, safeguards restored; see [phase3-us003-linkedin-publication-validation-2026-07-11.md](operations/phase3-us003-linkedin-publication-validation-2026-07-11.md). v1 text-only API (no LinkedIn image upload); article link preview image deferred to BL-009.
- Calendar reconciliation: stale item `scheduled` → `completed` via authoritative `campaign_id` without repeating pipeline
- Calendar `flow_a_completion` LinkedIn summaries (BL-003 / US-006–US-008) — **operationally validated** on `192.168.0.194` after deploy (`BUILD_REVISION=1784088086`): reopen of `2026-07-10-a-bounded-context-is-not-a-folder` to `scheduled` + `POST /editorial-calendar/execute-flow-a-due` (`dry_run=false`) reconciled with `linkedin_package_status=completed` and `linkedin_distribution_status=completed` from canonical campaign metadata; two remaining legacy completed rows with null summaries operator-patched once; all three calendar items now show non-null LinkedIn summaries
- Worker smoke and n8n import confirmed; n8n Flow A workflow is **activated on the Ubuntu server** under US-010 (repo export remains `active: false`; activation ≠ BL-005 unattended)
- Canonical Flow A n8n identity (**US-009** / BL-004 identification slice): **validated 2026-07-15** — export `n8n/workflows/silverman-blog-linkedin-flow-a-publish.json`, stable id `silvermanFlowAPublish01`, name **Silverman Blog LinkedIn Flow A Publish**. Evidence: [us-009 validation](operations/us-009-canonical-flow-a-n8n-identity-validation-2026-07-15.md).
- Flow A n8n activation (**US-010** / BL-004 activation slice): **validated 2026-07-15** — server `active: true`, Schedule Trigger `0 9 * * *` UTC, single-flight (static-data + shared-mount lockfile TTL 2h), idle restart + skip + TTL recovery with empty ready. Repo export `active: false`. Ready-folder HTTP path retained. Evidence: [us-010 validation](operations/us-010-flow-a-n8n-activation-validation-2026-07-15.md). *(Node count at US-010 was 31; later ready-path completion export is 35 — see implemented section.)*
- Ready-path completion HTTP + n8n wiring (`flow-a-ready-path-completion-http`): **deployed and re-imported** on `192.168.0.194` — `POST /complete-flow-a-ready-path` after schedule; server Set Configuration `git_publication` / `live_site_confirmation` / `update_calendar` true (repo export defaults remain `false`); n8n **35** nodes active. Operationally validated under BL-005 — see [bl-005-unattended-flow-a-validation-2026-07-15.md](operations/bl-005-unattended-flow-a-validation-2026-07-15.md).
- LinkedIn publication guard (**US-011** / BL-004 story 3): **validated 2026-07-15** — Flow A schedule ≠ LinkedIn enablement; `distribution_scheduled` ≠ LinkedIn API published; fail-closed `linkedin_publish_not_enabled` with temporary flag `false` then restore of recorded baseline `true`. US-011 is not permanent LinkedIn-off. Evidence: [us-011 validation](operations/us-011-linkedin-publication-guard-validation-2026-07-15.md).
- Fully unattended Flow A (**BL-005** / US-012–US-014): **validated 2026-07-16** — Manual Post A + Schedule Post B on remediated 35-node ready-path; both campaigns `flow_a_complete` with git+live, package, schedule, lifecycle, calendar; no LinkedIn API publish (variants `pending`). Schedule fire hit Pages-lag 404 then post-lag resume (US-002 pattern). Evidence: [bl-005-unattended-flow-a-validation-2026-07-15.md](operations/bl-005-unattended-flow-a-validation-2026-07-15.md).
- LinkedIn variant review policy (**US-015** / BL-006 story 1): **policy defined** — operator-facing Flow A strategy-driven publication default, optional `pending` supervision window, Flow A vs Flow B mandatory review distinction, blocked/deferred states. Evidence: [linkedin-variant-review-policy.md](operations/linkedin-variant-review-policy.md).
- LinkedIn cadence spacing policy (**US-051** / BL-021 story 1): **policy defined** (docs-only OpenSpec `ratify-linkedin-cadence-spacing-policy-us-051`) — ratifies US-020 same-campaign 72h spacing as normative; reaffirms LinkedIn fill ≈ ~2/local day via US-040K and blog frequency at strategy level; ratifies US-040K density + BL-019 gap as interim coexisting controls (density ≠ cadence; gap does not bypass cadence); defines **cadence conflict** for US-087–US-089 as the live `linkedin_publish_blocked_cadence` / related cadence-skip gate at `scheduled_at_utc` (or proposed slot). Evidence: [linkedin-cadence-spacing-policy.md](operations/linkedin-cadence-spacing-policy.md); pointer from [linkedin-publication-prerequisites.md](deployment/linkedin-publication-prerequisites.md#publish-time-sequence-and-cadence-guard-us-020). **Story accepted** (operator-accepted 2026-07-20). Console cadence-conflict warning (**US-087**), schedule-time shift-forward (**US-088**), and replan (**US-089**) are **implemented, deployed, and Story accepted** (see Implemented). **BL-021 closed 2026-07-20.** No worker publish-time cadence math, cron, n8n, or enablement changes from the US-051 policy change.
- LinkedIn publishing windows + shift-forward policy (**US-052** / BL-021 story 2): **policy defined** (docs-only OpenSpec `define-linkedin-publishing-windows-and-shift-forward-policy-us-052`) — preferred windows Tue–Thu / 08:00–10:00 or 16:00–18:00 America/Bogota (placement guidance; not a second 72h engine); strategy-level audience balancing with Flow A packaging unchanged; shift-forward when cadence-infeasible to next feasible slot (also US-040K max 2 + strategy constraints; no silent infeasible keep); residual cadence conflict still requires US-087 warning; fail-closed 28 operator-local-day horizon for US-088. Evidence: [linkedin-publishing-windows-and-shift-forward-policy.md](operations/linkedin-publishing-windows-and-shift-forward-policy.md); pointer from [linkedin-cadence-spacing-policy.md](operations/linkedin-cadence-spacing-policy.md); editorial `#linkedin-distribution-strategy`. **Story accepted** (operator-accepted 2026-07-20). Console cadence-conflict warning (**US-087**), schedule-time shift-forward (**US-088**), and replan (**US-089**) are **implemented, deployed, and Story accepted** (see Implemented). **BL-021 closed 2026-07-20.**
- Business and content metrics (**US-053** / BL-022 story 1): **definition published and Story accepted** (operator-accepted 2026-07-21; docs-only OpenSpec `define-business-and-content-metrics-us-053`) — normative operator definition of blog traffic (page views; unique visitors when available; top posts by views; referral/landing when available); LinkedIn reach and engagement (impressions/reach, reactions, comments, shares/reposts, engagement rate when computable) eligible only for **Live on LinkedIn** or an explicitly documented manual-post exception (never `distribution_scheduled` / pending / package-complete as reach); profile visits and audience growth (profile views, follower count, net follower change) with manual-first tracking procedure + durable log template; default calendar-month period and America/Bogota operator dates; blocked-state vocabulary (not configured / unavailable / not applicable / zero measured / blocked by publication honesty). Evidence: [business-and-content-metrics.md](operations/business-and-content-metrics.md); log template [business-and-content-metrics-log-TEMPLATE.md](operations/business-and-content-metrics-log-TEMPLATE.md); AC walk [us-053-business-and-content-metrics-doc-walk-2026-07-21.md](operations/us-053-business-and-content-metrics-doc-walk-2026-07-21.md). **BL-022 closed 2026-07-21.** Analytics platform/dashboard and required analytics auto-fetch worker routes remain **not implemented**. Does **not** mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED` or gate Flow A/B pipelines on metrics.
- Business outcome metrics (**US-054** / BL-022 story 2): **definition published and Story accepted** (operator-accepted 2026-07-21; docs-only OpenSpec `define-business-outcome-metrics-us-054`) — extends the same ops artifact for recruiter/executive conversations (two-way professional exchange; one-way likes/reactions remain US-053 engagement); job/consulting opportunities (concrete prospects; type + optional stage); high-performing topics/formats (thin operator criteria using US-053 traffic/reach/engagement where available + optional outcome attribution; MUST NOT invent ranks from schedule/pending/package-complete); US-054-specific **not applicable — none recorded** vs **zero (measured)** vocabulary; same calendar-month / America/Bogota period and review cadence. Evidence: [business-and-content-metrics.md](operations/business-and-content-metrics.md) §§6–8 / §11; log template [business-and-content-metrics-log-TEMPLATE.md](operations/business-and-content-metrics-log-TEMPLATE.md); AC walk [us-054-business-outcome-metrics-doc-walk-2026-07-21.md](operations/us-054-business-outcome-metrics-doc-walk-2026-07-21.md). **BL-022 closed 2026-07-21.** Does **not** mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED` or gate Flow A/B on outcome-log presence.
- Performance feedback collection (**US-055** / BL-023 story 1): **procedure published and Story accepted** (operator-accepted 2026-07-21; docs-only OpenSpec `collect-performance-feedback-consistently-us-055`) — extends the same ops artifact with consistent collection per measurement period (cadence after period close; period identity; value-or-blocked-state completeness; incomplete collection ≠ measured zero); thin theme/variant comparison from recorded eligible metrics (forbid ranks from schedule/pending/package-complete); effective-format identification reusing US-054 §8 high-performing criteria (no second contradictory table; no Flow A/B duplication). Evidence: [business-and-content-metrics.md](operations/business-and-content-metrics.md) §§14–16; log template [business-and-content-metrics-log-TEMPLATE.md](operations/business-and-content-metrics-log-TEMPLATE.md); AC walk [us-055-performance-feedback-collection-doc-walk-2026-07-21.md](operations/us-055-performance-feedback-collection-doc-walk-2026-07-21.md). **BL-023 closed 2026-07-21.** Does **not** introduce required analytics auto-fetch or a metrics dashboard; does **not** mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED` or gate Flow A/B on collection completeness.
- Performance planning feedback (**US-056** / BL-023 story 2): **procedure published and Story accepted** (operator-accepted 2026-07-21; docs-only OpenSpec `feed-performance-insights-into-planning-us-056`) — extends the same ops artifact with planning-feedback procedure: feed insights from recorded US-053 / US-054 / US-055 period evidence into future editorial planning (allowed inputs; durable log notes; planning decisions for next calendar-month / America/Bogota horizon); reduce repetition of low-performing content (inverse/complement of US-054 §8 / US-055 §16; signal or blocked state required; forbid ranks from schedule/pending/package-complete); human oversight fail-closed (MUST NOT auto-mutate strategy docs, editorial backlog, or Flow B discovery/draft/gap-trigger without explicit operator action; metrics-log notes ≠ backlog mutation). Evidence: [business-and-content-metrics.md](operations/business-and-content-metrics.md) §§17–19; log template [business-and-content-metrics-log-TEMPLATE.md](operations/business-and-content-metrics-log-TEMPLATE.md); AC walk [us-056-planning-feedback-doc-walk-2026-07-21.md](operations/us-056-planning-feedback-doc-walk-2026-07-21.md). **BL-023 closed 2026-07-21.** Does **not** redefine US-055 §§14–16; does **not** introduce required analytics auto-fetch, metrics dashboard, or auto-planning engine; does **not** mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED` or gate Flow A/B on planning-insight notes.
- **Flow B simplified process + gap policy (US-074 / US-075 / BL-016):** **policy defined** (docs-only OpenSpec `define-simplified-flow-b-us-074-075`) — Silverman Authority Manager, `blog-posts/pending-approval/` → `ready/`, weekly gap (next week, gap=0, max 2 drafts, spill A, n8n→HTTP intent, DeepSeek v1). Evidence: [flow-b-simplified-policy.md](operations/flow-b-simplified-policy.md); AC walk: [us-074-us-075-flow-b-policy-doc-walk-2026-07-19.md](operations/us-074-us-075-flow-b-policy-doc-walk-2026-07-19.md). **US-074/US-075 Story accepted** 2026-07-19; **BL-016 closed**.
- **Flow B gap operator settings (US-076 / BL-019 story 1):** **implemented and deployed 2026-07-19** on `192.168.0.194:8010` (`BUILD_REVISION=b89c429…`; OpenSpec archive `2026-07-19-persist-flow-b-gap-operator-settings-us-076`) — Postgres singleton table `flow_b_gap_operator_settings` in `silverman_linkedin_db` via existing `SILVERMAN_CALENDAR_DATABASE_URL` (`memory://` in tests); authenticated `GET`/`PUT /flow-b/gap-operator-settings` (OpenAPI live); internal `load_gap_operator_settings()` with documented defaults when row missing (`gap_trigger_enabled=false`, `gap_scan_mode=next_week`, friday/`15:00`, `min_lead_days=5`, `gap_posts_threshold=0`, `max_drafts_per_weekly_run=2`, `density_max_per_local_day=2`); Authority Manager **Gap settings** modal (static assets `index-CrAcUlTa.js` / `index-DqNDOQQX.css`); save MUST NOT enable LinkedIn API publish / MUST NOT mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED`. Live smoke: authenticated GET returned `source=defaults`, `gap_trigger_enabled=false`. Evidence: `tests/test_flow_b_gap_operator_settings.py`, Vitest `us076.gap-settings.test.tsx`. **Story accepted** (operator-accepted 2026-07-20).
- **Flow B calendar gap detect (US-077 / BL-019 story 2):** **implemented and deployed 2026-07-19** on `192.168.0.194:8010` (`BUILD_REVISION=52fa1c7…`; OpenSpec archive `2026-07-19-detect-flow-b-calendar-gaps-us-077`) — detect-only `detect_next_week_calendar_gaps()` + authenticated `GET /flow-b/calendar-gaps` scans the next operator-local Mon–Sun week; gap day = LinkedIn coverage ≤ `gap_posts_threshold` (default 0) for `pending`/`queued`/`published`; applies `min_lead_days` (default 5) from `load_gap_operator_settings()`; returns `gaps_found`/`no_gap`, `gaps[]`, target ISO week, operator timezone, settings source, `read_only=true`; detect MAY run when `gap_trigger_enabled=false`; MUST NOT mutate campaigns/calendar/drafts or start discovery/draft/trigger. Evidence: `tests/test_flow_b_calendar_gap_detect.py`; deploy verify OVERALL PASS; OpenAPI exposes GET `/flow-b/calendar-gaps`. **Story accepted** (operator-accepted 2026-07-20). BL-019 closed 2026-07-20.
- **Flow B topic discovery (US-078 / BL-017 story 1):** **implemented and deployed 2026-07-19** on `192.168.0.194:8010` (`BUILD_REVISION=18b6bec…`; OpenSpec archive `2026-07-19-discover-flow-b-topics-with-ai-us-078`) — authenticated `POST /flow-b/discover-topics` runs AI topic discovery constrained by career/authority objective (DeepSeek v1; provider-pluggable seam); inputs = authority brief + editorial canon topic spaces + soft anti-dup vs recent `blog-posts/processed/` + optional `prompts/flow-b/` primary material; BL-020 backlog not required; optional gap context (`target_week`, `empty_days[]`) informational only; batch size ≤ `max_drafts_per_weekly_run` from `load_gap_operator_settings()` (default 2); returns attachable `topics[]`; fail closed when no objective-aligned topic; MUST NOT write under `blog-posts/ready/` or `blog-posts/pending-approval/`. Docker image includes `content-strategy/` for editorial canon at runtime. Evidence: `tests/test_flow_b_topic_discovery.py`; deploy verify OVERALL PASS; OpenAPI exposes POST `/flow-b/discover-topics`. **Story accepted** (operator-accepted 2026-07-20). **BL-017 closed 2026-07-20.**
- **Flow B blog draft generation (US-079 / BL-017 story 2):** **implemented and deployed 2026-07-19** on `192.168.0.194:8010` (`BUILD_REVISION=c69b603…`; OpenSpec archive `2026-07-19-generate-flow-b-blog-draft-and-image-us-079`) — authenticated `POST /flow-b/generate-blog-drafts` accepts US-078 `topics[]` (+ optional gap context), generates Markdown via DeepSeek v1 (`BlogDraftGenerationProvider` seam), requests hero image via existing ComfyUI blog image path, writes `.md` + `.png` + `.flow-b.json` under `blog-posts/pending-approval/` only; batch ≤ `max_drafts_per_weekly_run`; blocking anti-AI heuristics at draft time; dry-run skips durable writes; MUST NOT write `ready/`, invoke Flow A publish/package/schedule, or call LinkedIn API publish. Evidence: `tests/test_flow_b_blog_draft_generation.py`; deploy verify OVERALL PASS; OpenAPI exposes POST `/flow-b/generate-blog-drafts`. **Story accepted** (operator-accepted 2026-07-20). **BL-017 closed 2026-07-20.**
- **Flow B blog draft approve/reject presentation (US-080 / BL-018 story 1):** **implemented and deployed 2026-07-19** on `192.168.0.194:8010` (`BUILD_REVISION=cb36fa9…`; OpenSpec archive `2026-07-19-present-flow-b-blog-drafts-for-approve-or-reject-us-080`) — authenticated `GET /flow-b/pending-approval-drafts` (+ detail + confined image) and `POST …/approve` / `…/reject` list/present packages from `blog-posts/pending-approval/` and record approve/reject in sidecar only; approve returns `promoted: false` / `promotion_pending: true` and MUST NOT move to `ready/`; reject remains non-publishable; Authority Manager **Flow B drafts** panel; no revision CMS / multi-round feedback / mandatory edit-apply; MUST NOT invoke Flow A publish/package/schedule or LinkedIn API publish. Evidence: `tests/test_flow_b_blog_draft_approval.py`, Vitest `us080.pending-drafts.test.tsx`; deploy verify OVERALL PASS; OpenAPI exposes pending-approval-drafts routes. **Story accepted** (operator-accepted 2026-07-20). **BL-018 closed 2026-07-20.**
- **Flow B blog draft promotion + spill A (US-081 / BL-018 story 2):** **implemented and deployed 2026-07-19** on `192.168.0.194:8010` (`BUILD_REVISION=34eb531…`; OpenSpec archive `2026-07-19-promote-approved-flow-b-blogs-onto-flow-a-path-us-081`; **Story accepted** (operator-accepted 2026-07-20)) — authenticated `POST /flow-b/pending-approval-drafts/{draft_id}/promote` moves approved `.md` + `.png` + `.flow-b.json` from `pending-approval/` → `ready/` with durable approval/promotion metadata (`origin: flow_b`); Authority Manager **Promote to ready/** affordance (static assets `index-CQysXXxM.js` / `index-zJhhRJLW.css`); after promote, Flow A publish/package/schedule/optional supervision are reused (campaign `flow` remains `flow_a`; no second mandatory LinkedIn gate); schedule strategy `flow_b_spill_a` (auto-select on Flow B gap provenance) under US-040K max 2; `publish_blog_post` rejects `blog-posts/pending-approval/` (`blog_publish_pending_approval_not_allowed`); promote MUST NOT invoke Flow A publish/package/schedule, Git publish, or LinkedIn API. Evidence: `tests/test_flow_b_blog_draft_promotion.py`, spill cases in `tests/test_linkedin_distribution_scheduling.py`, publish pending-approval rejection in `tests/test_blog_publish_flow.py`, Vitest promote cases in `us080.pending-drafts.test.tsx`. **BL-018 remains open until US-080+US-081 Story accepted.**
- **Flow B calendar gap trigger (US-082 / BL-019 story 3):** **implemented and deployed 2026-07-19** on `192.168.0.194:8010` (`BUILD_REVISION=652b73d…`; OpenSpec archive `2026-07-19-trigger-flow-b-draft-generation-on-calendar-gaps-us-082`; **Story accepted** (operator-accepted 2026-07-20)) — authenticated `POST /flow-b/gap-trigger` loads US-076 settings, enforces fail-closed `gap_trigger_enabled` (default false) + operator-local weekly window, runs US-077 detect, and on gaps claims ISO-week idempotency key `flow_b_gap_week:{operator_tz}:{YYYY}-W{ww}` (Postgres `flow_b_gap_trigger_batches` / `memory://` tests; failed may retry; stale `in_progress` reclaimable after 2h) then composes US-078 → US-079 into `blog-posts/pending-approval/` only (≤ `max_drafts_per_weekly_run`); clean no-ops for disabled / outside window / no-gap / idempotent; n8n Schedule→HTTP export `n8n/workflows/silverman-blog-linkedin-flow-b-gap-trigger.json` (`active: false`; HTTP Request only; ADR-0001); MUST NOT write `ready/`, approve/promote, or mark LinkedIn API published. Evidence: `tests/test_flow_b_calendar_gap_trigger.py`; deploy verify OVERALL PASS; OpenAPI exposes POST `/flow-b/gap-trigger`. **BL-019 remains open** until US-076+US-077+US-082 Story accepted.
- **Editorial content backlog (US-049 / BL-020 story 1):** **implemented, deployed, and Story accepted** (operator-accepted 2026-07-21) on `192.168.0.194:8010` (`BUILD_REVISION=be27655…`; OpenSpec archive `2026-07-21-create-editorial-content-backlog-us-049`) — Postgres table `editorial_content_backlog_items` in `silverman_linkedin_db` via existing `SILVERMAN_CALENDAR_DATABASE_URL` (`memory://` in tests); authenticated `GET`/`POST /editorial/content-backlog` and `GET`/`PUT /editorial/content-backlog/{item_id}` for topic/audience/objective/format/priority/status/target_date + LinkedIn derivative planning notes; empty list is success (store failure is distinct 503); create/update MUST NOT enable LinkedIn API publish / MUST NOT mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED` / MUST NOT write `linkedin-posts/` packages; optional enrichment only — Flow B discovery / draft / gap-trigger modules do not import or require the backlog. Authority Manager **Content backlog** modal. Evidence: `tests/test_editorial_content_backlog.py`, Vitest `us049.content-backlog.test.tsx`; live OpenAPI exposes backlog routes. **BL-020 closed 2026-07-21.**
- **Editorial content backlog dependencies + prioritization (US-050 / BL-020 story 2):** **implemented, deployed, and Story accepted** (operator-accepted 2026-07-21) on `192.168.0.194:8010` (`BUILD_REVISION=be27655…`; OpenSpec archive `2026-07-21-add-editorial-content-backlog-dependencies-us-050`) — Extends backlog items with `depends_on_item_ids` (stable `item_id` refs; cycle/dangling/self rejected with structured 4xx) and `queue_rank` (lower = earlier); list sort = `queue_rank ASC`, priority band (`high` < `medium` < `low`), then `updated_at_utc DESC`; authenticated `PUT /editorial/content-backlog/reorder` reassigns contiguous ranks; create/update/reorder MUST NOT enable LinkedIn API publish / MUST NOT mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED` / MUST NOT write `linkedin-posts/` packages; unused deps/ranks MUST NOT gate Flow B discovery / draft / gap-trigger. Authority Manager Content backlog modal shows dependency labels, dependency multi-select, priority change, and move earlier/later (static assets rebuilt: `index-Dd92hzfG.js` / `index-BRVrIP7S.css`). Evidence: `tests/test_editorial_content_backlog.py`, Vitest `us050.content-backlog.test.tsx`; live OpenAPI exposes reorder. Discovery seed/override remains out of scope. **BL-020 closed 2026-07-21.**
- LinkedIn variant quality criteria (**US-016** / BL-006 story 2): **criteria defined** — operator-facing quality and differentiation criteria, normative default variant audience/objective mapping, supervision-window checklist, criteria-failure vs technical-block communication; campaign `variants[]` includes `objective` and `audience_lens` at package generation. Evidence: [linkedin-variant-quality-criteria.md](operations/linkedin-variant-quality-criteria.md).
- LinkedIn variant supervision mechanics (**US-017** / BL-006 story 3): **implemented** (worker routes + docs + tests) — `POST /correct-linkedin-variant`, `POST /defer-linkedin-variant`, extended `POST /cancel-linkedin-publication` for `pending` pre-queue cancel; `operator_supervision` metadata on `variants[]`. Not operationally validated in production smoke. Evidence: [linkedin-variant-supervision-mechanics.md](operations/linkedin-variant-supervision-mechanics.md). **BL-006 closed** when US-015, US-016, and US-017 acceptance criteria demonstrated (2026-07-16).
- **BL-007 / US-018 scheduled LinkedIn publication execution:** opt-in `auto_queue_pending` on publish-due identifies due `pending` variants, applies US-017 supervision exclusions, queues through the existing safety-delay service, and preserves once-only publish behavior. Operator script and manual inactive HTTP-only n8n export are included. **Operationally validated 2026-07-16** on `192.168.0.194` (`BUILD_REVISION=c7bce02`): dry-run smoke with zero campaign mutation, then controlled real window — one due variant queued+published once with URN evidence and idempotent repeat run. Evidence: [us-018 validation](operations/us-018-scheduled-linkedin-publication-validation-2026-07-16.md). Not unattended: no cron/schedule wired; n8n publish-pending export stays `active: false`.
- **BL-007 / US-019 publication evidence formalization:** complete success evidence (`linkedin_post_urn`, `published_at`, `linkedin_publication` with `provider`/`post_urn`/`published_at`/`http_status`) and failure-context taxonomy; additive URN/`published_at` on `auto_queue_results`. **Operationally validated 2026-07-17** on `192.168.0.194` (`BUILD_REVISION=3c4d9f5`): real publish of `executive-recruiter` on `flow-a-2026-07-10-deferring-is-not-avoiding-it-can-be-architecture` wrote URN `urn:li:share:7483704861348519936` with `http_status=201`; replay idempotent. Evidence: [us-019/us-020 validation](operations/us-019-us-020-linkedin-publication-validation-2026-07-17.md).
- **BL-007 / US-020 publish-time sequence and cadence guard:** per-campaign guard at publish time (plain / combined / targeted / scan) plus auto-queue sequence pre-filter; 72h cadence anchored to stored `published_at`; evidence fail-closed; stable block reasons. **Operationally validated 2026-07-17** on `192.168.0.194` (`BUILD_REVISION=3c4d9f5`): dry-run zero mutation with cadence + sequence reasons; real cadence block without LinkedIn call; real sequence auto-queue skip; post-publish cadence after allowed publish. Evidence: [us-019/us-020 validation](operations/us-019-us-020-linkedin-publication-validation-2026-07-17.md). **BL-007 closed.**
- LinkedIn retry and recovery classification (**US-021** / BL-008 story 1): **policy defined 2026-07-16, operationally exercised 2026-07-17, operator-accepted** — normative classification of publication outcomes (recoverable transient / recoverable after remediation / non-recoverable as-is / uncertain, keyed on `last_error_code` + `http_status`; unlisted combinations fail safe to uncertain), blocked as a separate non-failure class, token-renewal-precedes-re-queue rule, and mandatory operator verification on LinkedIn before re-queueing uncertain outcomes. Uncertain class and the operator-verification procedure exercised live during the US-022 validation. Evidence: [linkedin-retry-recovery-classification.md](operations/linkedin-retry-recovery-classification.md), [us-021/us-022 validation](operations/us-021-us-022-linkedin-retry-recovery-validation-2026-07-17.md).
- LinkedIn article preview input verification (**US-023** / BL-009 story 1): **operationally validated 2026-07-17, operator-accepted** on `192.168.0.194` (`BUILD_REVISION=d15d85b`): initial dry-run detected a real live-site failure (`linkedin_preview_validation_og_tags_missing` — no `og:image`; `linkedin_preview_validation_og_description_mismatch` — excerpt-based description) on both 2026-07-15 campaigns; remediation on the public blog repo (`silverberdi.github.io` commit `e4d10de`: `og:image`/`twitter:image` from `page.image`, description tags prefer `page.description`, `_config.yml` url set to `https://silverman.pro`) with approved commit/push; passing dry-run + real runs persisted `linkedin_article_preview_validation` evidence with no other field changes. Zero LinkedIn API involvement (US-024 boundary). Evidence: [us-023 validation](operations/us-023-linkedin-preview-input-validation-2026-07-17.md).
- Flow A operational alerts (**US-028 + US-029 + US-030 / BL-011**): **deployed and operator-accepted 2026-07-17** on `192.168.0.194` (`BUILD_REVISION=b67c538`). **BL-011 closed 2026-07-17.** Controlled live smoke: evaluate returned live US-028 candidates (`item_moved_to_error`, `blog_publication_failure`); report ingest produced `failed_n8n_workflow`; eight-type `summary.counts`; evaluate-only zero lifecycle mutation; emit with flags unset → `emission.status=disabled`; auth 401; smoke entry cleaned up. `unhealthy_worker` covered by local degraded fixtures (healthy live folders correctly yield count 0). US-029 types covered by fixtures + live eight-type contract. **Follow-up enabled 2026-07-17:** emit flags on; n8n webhook + Error Trigger + evaluate/emit schedule active; live emit smoke `emission.status=emitted` (2 fingerprints). Evidence: [flow-a-operational-alerts.md](operations/flow-a-operational-alerts.md).

## Implemented but not operationally validated

- Flow A incomplete-campaign recovery (**US-031 + US-032 / BL-012**): **implemented, automated-tested, deployed, and operator-accepted 2026-07-18** — authenticated inspect/resume/repair/cancel under `/flow-a/incomplete-campaign-recovery` on `192.168.0.194` (`BUILD_REVISION=018aa36`); additive US-032 recovery-action taxonomy (`recommended_recovery_action`), durable `flow_a_recovery.attempts` ledger, and cancel gating. Inspect remains read-only; dry-run does not persist cancel state or attempts. **BL-012 closed 2026-07-18.** Evidence: [flow-a-incomplete-campaign-recovery.md](operations/flow-a-incomplete-campaign-recovery.md), [bl-012 acceptance](operations/bl-012-incomplete-campaign-recovery-acceptance-2026-07-18.md), `tests/test_flow_a_incomplete_campaign_recovery.py`.
- Flow A concurrency and duplicate-execution protection (**US-035 / BL-013 story 3**): **implemented, automated-tested, acceptance criteria validated (2026-07-18), and deployed** on `192.168.0.194` (`BUILD_REVISION=018aa36`) — restart-interruption fixtures validated earlier; live worker now includes concurrency protections. **BL-013 closed 2026-07-18.** Evidence: [flow-a-concurrency-duplicate-execution-protection-us-035.md](operations/flow-a-concurrency-duplicate-execution-protection-us-035.md), `tests/test_flow_a_concurrency_us035.py`.
- Flow A concurrency and duplicate-execution protection (**US-034 / BL-013 story 2**): **implemented, automated-tested, acceptance criteria validated (2026-07-18), and deployed** on `192.168.0.194` (`BUILD_REVISION=018aa36`). **BL-013 closed 2026-07-18.** Evidence: [flow-a-concurrency-duplicate-execution-protection-us-034.md](operations/flow-a-concurrency-duplicate-execution-protection-us-034.md), `tests/test_flow_a_concurrency_us034.py`.
- Flow A concurrency and duplicate-execution protection (**US-033 / BL-013 story 1**): **implemented, automated-tested, acceptance criteria validated (2026-07-18), and deployed** on `192.168.0.194` (`BUILD_REVISION=018aa36`). **BL-013 closed 2026-07-18.** Evidence: [flow-a-concurrency-duplicate-execution-protection-us-033.md](operations/flow-a-concurrency-duplicate-execution-protection-us-033.md), `tests/test_flow_a_concurrency_us033.py`.
- Editorial backup scope, retention, and integrity (**US-036 / BL-014 story 1**): **implemented, automated-tested, operator-accepted 2026-07-18, and deployed** on `192.168.0.194` (`BUILD_REVISION=018aa36`) (fixture evidence) — operator policy defines included/excluded scope, retention (7 most recent pass packages; never auto-delete fail/blocked), and integrity verification (`pass`/`fail`/`blocked`) under `metadata/backups/<backup_id>/`; library + CLI (`scripts/editorial_backup.py`) for verify (required) and optional create/prune writing only under `metadata/backups/`; no new FastAPI routes; `GET /health` unchanged (folder readiness only). Evidence: [editorial-backup-scope-retention-integrity.md](operations/editorial-backup-scope-retention-integrity.md), [bl-014 acceptance](operations/bl-014-editorial-backup-restore-acceptance-2026-07-18.md), `tests/test_editorial_backup_integrity.py`.
- Editorial backup restore and recovery procedure (**US-037 / BL-014 story 2**): **implemented, automated-tested, operator-accepted 2026-07-18, and deployed** on `192.168.0.194` (`BUILD_REVISION=018aa36`) (fixture restore drills + recovery runbook). **BL-014 closed 2026-07-18.** Library `editorial_backup_restore.py` + CLI (`restore-dry-run`, `restore-drill`, gated `restore`) require US-036 integrity `pass` before writes; dry-run/fixture-first; live restore fail-closed without `--i-understand-live-restore`; secret-safe `pass`/`fail`/`blocked` with `restore_*` reason codes; covers US-036 included classes. No new FastAPI restore routes. Live production restore remains operator-gated and was not required for acceptance. Evidence: [editorial-backup-restore-recovery.md](operations/editorial-backup-restore-recovery.md), [bl-014 acceptance](operations/bl-014-editorial-backup-restore-acceptance-2026-07-18.md), `tests/test_editorial_backup_restore.py`.
- Flow A operational status (**US-026 + US-027 / BL-010**): **deployed and operator-accepted 2026-07-17** on `192.168.0.194` (`BUILD_REVISION=b67c538`). **BL-010 closed 2026-07-17.** Controlled live smoke of authenticated `GET /flow-a/operational-status`: `status=partial` with 46 successful runs, 6 campaigns (4 successful / 1 failed / 1 blocked / 1 in progress), LinkedIn publish_state counts, stage-duration aggregates (`campaigns_with_stage_durations=6`, `executions_with_duration=46`), `github_pages_checkout` dependency failure, auth 401, invalid `now_utc` 422, deterministic repeat GET, byte-for-byte zero mutation (128 files). Evidence: [flow-a-operational-status.md](operations/flow-a-operational-status.md).
- LinkedIn retry limits and recovery evidence (**US-022** / BL-008 story 2): **operationally validated 2026-07-17 (`BUILD_REVISION=d15d85b`) on the primary recovery chain, operator-accepted. BL-008 closed 2026-07-17.** Demonstrated on real variant `a-bounded-context :: engineering-leadership`: controlled transport failure (container-scoped hosts injection, reverted) → `failed` with `linkedin_publish_api_error`/`http_status null` → classified `uncertain` → blind and wrong-class re-queues rejected with stable codes and zero mutation → operator-attested `linkedin_post_absence_verified` re-queue (recovery event appended) → successful retry (`urn:li:share:7483974070842241024`, attempt #2, 1 retry consumed) → failed attempt retained in append-only history → replay idempotent. Correction (`/correct-linkedin-variant`), failed-cancellation, exhaustion, and legacy normalization remain at unit-test scope. Evidence: [us-021/us-022 validation](operations/us-021-us-022-linkedin-retry-recovery-validation-2026-07-17.md). Per-variant retry budget (initial + 2 manual retries; only real LinkedIn API calls count), append-only `linkedin_publication_attempts` and `linkedin_recovery_history` evidence, class-aware re-queue with `recovery_confirmation` enum on `POST /queue-linkedin-publication`, failed-content correction via `POST /correct-linkedin-variant` (content-invalid class only), and `failed → cancelled` via `POST /cancel-linkedin-publication` with full evidence preservation. The previously recorded US-021 divergence is resolved in code: manual re-queue of a `failed` variant no longer clears stored `linkedin_publication` evidence. No new endpoints, env vars, or `publish_state` values; enablement guard unchanged. Evidence: [linkedin-retry-recovery-classification.md](operations/linkedin-retry-recovery-classification.md), unit suites `tests/test_linkedin_publication.py` / `tests/test_linkedin_supervision_flow.py`.
- LinkedIn article preview rendering confirmation (**US-024** / BL-009 story 2): **operationally demonstrated 2026-07-17, operator-accepted.** Procedure executed end-to-end on a real campaign: pre-publish Post Inspector attempt recorded as `confirmation_blocked` (LinkedIn-side app crash across browsers; site-side crawler accessibility verified); post-publish observation of real post `urn:li:share:7483953784612786177` (`keep-contracts-boring :: executive-recruiter`, published 2026-07-17T18:40:27Z via the guarded path) with passing US-023 inputs showed no article card → decision-matrix outcome `preview_not_rendered_post_format` (v1 text-post format observation, not an input or procedure failure). Completed evidence records: [blocked attempt](operations/us-024-preview-confirmation-blocked-2026-07-17.md), [post-publish confirmation](operations/us-024-preview-confirmation-keep-contracts-boring-2026-07-17.md). Procedure doc: [linkedin-preview-rendering-confirmation.md](operations/linkedin-preview-rendering-confirmation.md).
- LinkedIn article preview fallback policy (**US-025** / BL-009 story 3): **operationally demonstrated 2026-07-17, operator-accepted. BL-009 closed 2026-07-17** — inputs verified, rendering observed (`preview_not_rendered_post_format`: v1 text post renders no article card), reaction executed per policy; `content.article` remains a named deferred future-change candidate. Fallback decision executed end-to-end on the recorded `preview_not_rendered_post_format` trigger (`keep-contracts-boring`): post-publish default **accept and record** (`fallback_accept_rendering`) with zero endpoint calls, campaign document sha256 unchanged, zero retry-budget consumption, no `recovery_confirmation` use; escalation recorded as `fallback_format_change_deferred` naming the `content.article` future-change preconditions (now backed by one operationally recorded triggering outcome). Canonical spec `linkedin-article-preview-fallback` synced 2026-07-17. Evidence: [us-025 fallback decision](operations/us-025-preview-fallback-decision-keep-contracts-boring-2026-07-17.md). Policy doc: [linkedin-preview-fallback-policy.md](operations/linkedin-preview-fallback-policy.md).
- OAuth LinkedIn token refresh in production (refresh token not present in current token store)
- **BL-032 / US-083** honest LinkedIn publication status + action availability (control-center **foundation**): **implemented and deployed 2026-07-20** on `192.168.0.194:8010` (OpenSpec archive `2026-07-20-show-honest-linkedin-publication-status-and-actions-us-083`; git `801f1cb`; live console assets `index-C4S9vr3i.js` / `index-BEnefPLS.css`; deploy verify OVERALL PASS) — Silverman Authority Manager primary labels: **Scheduled** / **Waiting to send** / **Live on LinkedIn** / **Failed** / **Cancelled** (technical `publication_state` / `publish_state` secondary in EventModal diagnostics); Waiting to send / `queued` helper makes not-LinkedIn-API-published unmistakable; blog **Published on blog** remains visually/verbally distinct from Live on LinkedIn; EventModal **What you can do now** matrix wires existing eligibility (edit / cancel-pending / reschedule-defer / reopen + `canMutate`); publish-now ownership moved to US-086 (see below). Cancel-queued is owned by US-085. **Story accepted** (operator-accepted 2026-07-20). **BL-032 closed 2026-07-20.** Does **not** reopen BL-015.
- **BL-032 / US-084** postpone/reschedule from console for not-Live LinkedIn variants (**Scheduled/`pending` and Waiting to send/`queued`**): **implemented and deployed 2026-07-20** on `192.168.0.194:8010` (OpenSpec archive `2026-07-20-postpone-and-reschedule-linkedin-variants-from-console-us-084`; git `8184d2d`; live console assets `index-DzxHvZB-.js` / `index-BEnefPLS.css`; deploy verify OVERALL PASS; `BUILD_REVISION=8184d2dff224…`) — extends `POST /defer-linkedin-variant` to accept `queued` (keeps `queued`, updates `scheduled_at_utc` + `publish_after_utc`, deferral history; pending path unchanged); schedule-visibility `schedule_editable` for pending and queued; deliberate EventModal / ScheduleEditor postpone control with US-083 preview vs real honesty; real defer refreshes calendar to new local slot; density/cadence refusals include plain next step; publish-now is owned by US-086 (distinct from postpone); cancel for pending + queued is owned by US-085 (distinct from postpone); no LinkedIn API publish; BL-015 stays closed. Vitest `us084.postpone-reschedule.test.tsx` + pytest defer/visibility coverage. **Story accepted** (operator-accepted 2026-07-20).
- **BL-032 / US-085** cancel LinkedIn variants that are **not yet Live** from the console (**Scheduled/`pending` and Waiting to send/`queued`**): **implemented and deployed 2026-07-20** on `192.168.0.194:8010` (OpenSpec archive `2026-07-20-cancel-linkedin-variants-not-yet-live-from-console-us-085`; live with US-086 rebuild git `7b4561b`; assets `index-DjTtqvO6.js` / `index-BEnefPLS.css`; deploy verify OVERALL PASS) — reuses authenticated `POST /cancel-linkedin-publication` only (no second cancel pipeline; no LinkedIn API call; cancel is not blocked solely by `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED`); EventModal deliberate Cancel for pending (supervision join) and queued (schedule-visibility `campaignId`/`variantId` without pending-supervision join); US-083 preview vs real + explicit confirmation for real cancel; after real cancel status **Cancelled**, publish/cancel-pending/cancel-queued withdrawn, reopen remains restore path (US-040J); action matrix makes `cancel_queued` available when eligible; publish-now is owned by US-086 (distinct from cancel); Vitest `us085.cancel-not-yet-live.test.tsx`. Worker cancel SoT confirmed unchanged (pending→cancelled `pre_queue`, queued→cancelled `post_queue`). **Story accepted** (operator-accepted 2026-07-20). Does **not** reopen BL-015.
- **BL-032 / US-086** publish LinkedIn variants **immediately from the console** for eligible not-Live items (**Waiting to send/`queued` primary; Scheduled/`pending` via `auto_queue_pending` + `publish_now`**): **implemented and deployed 2026-07-20** on `192.168.0.194:8010` (OpenSpec archive `2026-07-20-publish-linkedin-variant-immediately-from-console-us-086`; git `7b4561b`; live console assets `index-DjTtqvO6.js` / `index-BEnefPLS.css`; deploy verify OVERALL PASS) — sole mutation SoT remains authenticated `POST /publish-linkedin-due-variants` (typed `publishDueVariant` client; no second publish pipeline; no browser filesystem SoT; no n8n Execute Command); EventModal deliberate **Publish now** with US-083 preview vs real + explicit confirmation for real; real success shows **Live on LinkedIn** + traceable `linkedin_post_urn`; fail-closed when `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED` is off; plain-language cadence/sequence/config blocks; action matrix makes `publish_now` available when eligible + `canMutate` (supersedes “not available yet”); postpone (US-084) and cancel (US-085) remain distinct; optional schedule-visibility `linkedin_post_urn` for Live re-open verification; Vitest `us086.publish-now.test.tsx`. **Story accepted** (operator-accepted 2026-07-20). Does **not** reopen BL-015.
- **BL-021 / US-087** Show Cadence Conflicts Visually in the LinkedIn Console: **implemented and deployed 2026-07-20** on `192.168.0.194:8010` (OpenSpec archive `2026-07-20-show-linkedin-console-cadence-conflict-warning-us-087`; git `f97920a`; live console assets `index-DW7TiD4B.js` / `index-VJdhmMw8.css`; deploy verify OVERALL PASS) — authenticated `GET /flow-a/schedule-visibility` projects additive `cadence_conflict` / `cadence_conflict_code` / `cadence_earliest_feasible_at_utc` using shared US-020 `CADENCE_MINIMUM_INTERVAL` (72h) at each not-yet-Live LinkedIn item’s `scheduled_at_utc` (cadence-only; Live / Cancelled / Failed / blog / evidence-invalid-alone / density-alone stay false); Silverman Authority Manager Week + Month show a red `cadence-conflict-indicator` beside US-083 primary labels; EventModal plain-language cadence explanation + earliest feasible local time / Postpone / Replan cadence conflicts (US-089 next-step honesty updated when replan ships — see US-089; warning chrome unchanged); pytest `tests/test_flow_a_schedule_visibility.py` + Vitest `us087.cadence-conflict-warning.test.tsx`. Does **not** change publish-time cadence evaluation, n8n, cron, OAuth, or `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED`. **Story accepted** (operator-accepted 2026-07-20). **BL-021 closed 2026-07-20.**
- **BL-021 / US-088** Schedule LinkedIn Variants With Cadence-Aware Shift Forward: **implemented and deployed 2026-07-20** on `192.168.0.194:8010` (OpenSpec archive `2026-07-20-schedule-linkedin-variants-with-cadence-aware-shift-forward-us-088`; git `fa90a7f`; `BUILD_REVISION=fa90a7fd86a7…`; deploy verify OVERALL PASS) — `schedule_linkedin_distribution` / `POST /schedule-linkedin-distribution` for `flow_a_staggered` and `flow_b_spill_a` evaluates same-campaign US-020/US-087 cadence via shared `CADENCE_MINIMUM_INTERVAL` / `project_cadence_conflict_at` (no second 72h engine); shifts forward to the next feasible slot (US-040K max 2/local day + stagger ≥3 calendar days / spill empty-day→week→forward); prefers US-052 windows while scanning; fail-closed within 28 operator-local days with `linkedin_schedule_no_feasible_slot`; MUST NOT call LinkedIn API publish or bypass `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED`; US-087 residual `cadence_conflict` projection unchanged. Evidence: `src/silverman_blog_linkedin/linkedin_schedule_feasibility.py`, pytest US-088 cases in `tests/test_linkedin_distribution_scheduling.py`; policy pointer in [linkedin-publishing-windows-and-shift-forward-policy.md](operations/linkedin-publishing-windows-and-shift-forward-policy.md). **Story accepted** (operator-accepted 2026-07-20). **BL-021 closed 2026-07-20.**
- **BL-021 / US-089** Replan Already-Scheduled LinkedIn Variants That Conflict With Cadence: **implemented and deployed 2026-07-20** on `192.168.0.194:8010` (OpenSpec archive `2026-07-20-replan-already-scheduled-linkedin-cadence-conflicts-us-089`; git `e603b20`; `BUILD_REVISION=e603b200b4d9…`; live console assets `index-DPqdVRPa.js` / `index-VJdhmMw8.css`; deploy verify OVERALL PASS; OpenAPI exposes `/replan-linkedin-cadence-conflicts`) — authenticated `POST /replan-linkedin-cadence-conflicts` (`dry_run` default **true**) selects not-yet-Live `pending`/`queued` variants that are cadence-infeasible at current `scheduled_at_utc` (US-051 / US-087 meaning only) and shifts them forward via shared US-088 `linkedin_schedule_feasibility.find_feasible_slot_forward` (preferred windows + 28 operator-local-day horizon + density `planned_counts`; no second 72h engine); queued keeps `queued` and aligns `publish_after_utc`; all-or-nothing real apply after a fully feasible plan; fail-closed `linkedin_schedule_no_feasible_slot`; MUST NOT call LinkedIn API publish or bypass `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED`. Thin console affordance in EventModal (Preview vs Make real) + schedule-visibility refresh clears US-087 warnings when the new slot is feasible; EventModal next-step no longer claims replan is unshipped. Evidence: `src/silverman_blog_linkedin/linkedin_cadence_replan.py`, pytest `tests/test_linkedin_cadence_replan.py`, Vitest `us089.replan-cadence-conflicts.test.tsx`; ops curl pointer in [linkedin-publishing-windows-and-shift-forward-policy.md](operations/linkedin-publishing-windows-and-shift-forward-policy.md). **Story accepted** (operator-accepted 2026-07-20). **US-051 / US-052 / US-087 / US-088 / US-089 / BL-021 Story accepted / closed 2026-07-20.** Does **not** expand into BL-022.

## Manual steps (by design)

- Git commit and push to GitHub Pages when automatic Git publication is disabled, not opted in (`git_publication: false`), or validation window is closed — manual fallback after worker handoff
- LinkedIn draft review and manual publish (Flow B path) or guarded API publish when enabled
- n8n workflow activation when operator chooses unattended orchestration
- Editorial source placement in `blog-posts/ready/`

## Incomplete / deferred

- **Operational secrets permissions + secrets-absence review (US-058 / BL-024 story 2):** **procedure published and Story accepted** (operator-accepted 2026-07-21; docs-only OpenSpec `review-operational-secrets-permissions-us-058`) — live least-privilege + secrets-absence review on `192.168.0.194`; remediations: deleted live-looking `worker_api_key` copies under `/home/silverman/n8n-imports/` (kept `*.source.json` placeholders); replaced empty deploy-key/`known_hosts` dirs with durable `600` files + GitHub deploy key registered/verified (`git ls-remote` OK). Evidence: [operational-secrets-permissions-review.md](operations/operational-secrets-permissions-review.md); [us-058-permissions-review-2026-07-21.md](operations/us-058-permissions-review-2026-07-21.md). **US-057** key rotation remains **deferred**.
- **Operational secrets ownership + rotation cadence (US-059 / BL-024 story 3):** **procedure published and Story accepted** (operator-accepted 2026-07-21; docs-only OpenSpec `define-operational-secrets-ownership-cadence-us-059`) — system-owner ownership; cadences 90d (worker/n8n API key + US-058 re-audit), 180d (provider keys + LinkedIn client secret), 1y (deploy key), immediate on suspected exposure; blocked/deferred vocabulary. Evidence: [operational-secrets-ownership-cadence.md](operations/operational-secrets-ownership-cadence.md). **BL-024 closed 2026-07-21** (US-057 waived + US-058 + US-059 Story accepted). Does **not** execute live rotations, formalize BL-025 token lifecycle, absorb BL-026, or mutate `SILVERMAN_LINKEDIN_PUBLICATION_ENABLED`.
- **BL-031 / US-041** Persist editorial calendar in PostgreSQL database **`silverman_linkedin_db`**: **Closed / Story accepted 2026-07-19** after prior cutover smoke on `192.168.0.194:8010` (git `.build_git_sha` `43f773e`; health `calendar_store=postgres:silverman_linkedin_db`, `calendar_store_ready=true`, `calendar_database=silverman_linkedin_db`). ADR-0004 accepted. `load_calendar` / `save_calendar_atomic` use configured store (`SILVERMAN_CALENDAR_DATABASE_URL`); legacy `calendar.json` is import-only. BL-014 filesystem packages no longer treat `calendar.json` as calendar SoT. Operator cutover checklist: [silverman-linkedin-db-calendar-cutover.md](operations/silverman-linkedin-db-calendar-cutover.md). Does **not** restore historically wiped schedule rows. Fresh LAN re-probe not run at acceptance time (operator not on same network).
- Flow B gap trigger remains **operator-gated** (US-082 Story accepted): `gap_trigger_enabled` default **false**; n8n gap-trigger export remains `active: false` until operator enables unattended weekly runs.
- Dairector content paths
- **BL-032** Turn the LinkedIn Console Into an Operator Control Center — **US-083 foundation implemented and deployed 2026-07-20** on `192.168.0.194:8010` (see Implemented; live assets `index-C4S9vr3i.js` / `index-BEnefPLS.css`; **Story accepted** (operator-accepted 2026-07-20) — operator-accepted 2026-07-20). **US-084 postpone/reschedule implemented and deployed 2026-07-20** (pending + queued via extended defer; live assets `index-DzxHvZB-.js` / `index-BEnefPLS.css`; git `8184d2d`; **Story accepted** (operator-accepted 2026-07-20) — see Implemented). **US-085 cancel for not-yet-Live (`pending` + `queued`) implemented and deployed 2026-07-20** (see Implemented; live assets `index-DjTtqvO6.js` / `index-BEnefPLS.css`; git `7b4561b`; **Story accepted** (operator-accepted 2026-07-20)). **US-086 publish now for eligible not-Live (`queued` + `pending` via auto_queue+publish_now) implemented and deployed 2026-07-20** (see Implemented; live assets `index-DjTtqvO6.js` / `index-BEnefPLS.css`; git `7b4561b`; **Story accepted** (operator-accepted 2026-07-20)). BL-015 stays closed (not reopened).
- **BL-015** Flow A LinkedIn variant supervision console — **Closed 2026-07-19** after **US-038–US-040M Story accepted** by operator (final gate US-040L filters-modal walkthrough; live assets `index-DfOv3r_J.js` / `index-IGjH0V3D.css`, `BUILD_REVISION=8f820894dfad…`; not unattended production). Capability detail (US-038–US-040 + US-040A–US-040K): **US-038 (Story 1) + US-039 (Story 2) + US-040 (Story 3) implemented in worker:** authenticated `GET /flow-a/linkedin-variants/pending-supervision` aggregates `publish_state=pending` variants (calendar join, nullable `draft_content`, deferred/`auto_queue_eligible`/`operator_supervision_reason`, `issues[]`, secret-safe `integration_failures[]` for failed siblings); same-origin console at `GET /flow-a/console/linkedin-variant-supervision` lists pending rows and offers edit/defer/cancel that call existing US-017 `POST /correct-linkedin-variant`, `POST /defer-linkedin-variant`, and `POST /cancel-linkedin-publication` (dry-run default on; confirm for real cancel; enablement display-only). **US-040A implemented in worker (console-layer only; Story accepted 2026-07-19; BL-015 remains open):** React + TypeScript + Vite app under `frontend/linkedin-variant-supervision-console/` builds static artifacts into `src/silverman_blog_linkedin/static/linkedin-variant-supervision-console/` (worker serves `index.html` + `/assets/*`; no separate frontend server; build with `npm ci && npm run build` before Docker/`COPY src`); list remains first-class with Stories 1–3 parity; typed injectable-auth API client; shared normalized model. **US-040B implemented (list + month schedule visibility; Story accepted 2026-07-19; BL-015 remains open):** authenticated read-only `GET /flow-a/schedule-visibility?year=&month=` aggregates Flow A blog calendar items + LinkedIn variant schedules (`items[]` with channel, identity, `scheduled_at_utc`, operator-facing `publication_state`, blocked/critical, `linkedin_api_published` only with API evidence); dual first-class List + Month calendar views with persistent switcher (filters/dry-run/selected context survive; unsaved schedule-draft warn); CSS-grid month UX (prev/next, today, selected/empty days, UTC day-bucketing + local times, mobile agenda expansion); shared filters (channel/campaign/publication state/blocked/due-soon) with discoverable hidden critical failures; dark theme; Vitest + pytest coverage; pending-supervision + US-017 POSTs unchanged. **US-040C implemented (schedule modification from calendar; Story accepted 2026-07-19; BL-015 remains open):** shared `ScheduleEditor` from List / Month / mobile agenda; LinkedIn schedule commits reuse `POST /defer-linkedin-variant` only (optional `actor`/`source` audit; interim duplicate-slot + same-day/72h saturation codes); blog commits via authenticated `POST /editorial-calendar/update-item-schedule` (dry-run default, fingerprint conflict → `calendar_completion_concurrent_update`, `save_calendar_atomic`, schedule-change audit + idempotency); additive `schedule_editable` / `calendar_fingerprint` on schedule-visibility without rewriting US-040B baseline; dry-run visible + confirm for real; post-success refresh of schedule-visibility + pending-supervision with previous/new + related LinkedIn variants as separate overrides by default; no LinkedIn API publish / blog publish on schedule edit; interim BL-021 cadence rules until BL-021 closes. **US-040D implemented (public URL + Google auth readiness only; Story accepted 2026-07-19; BL-015 remains open):** authn/authz remain behind typed `SupervisionApiClient` + worker `require_api_key` middleware with injectable `AuthProvider` (`canRead`/`canMutate`; MemoryBearer for local ops; OIDC bearer or cookie/`credentials` swap without calendar/list/ScheduleEditor churn); UI session states anonymous / authenticated / expired-session / forbidden / service-unavailable; mutation gating for anonymous and read-only sessions; mobile expiry preserves visible list/calendar context and unsaved schedule drafts with Sign-in re-auth; same-origin relative API paths by default; restrictable CORS allowlist documented for a future distinct-origin SPA (permissive CORS **not** enabled); no hardcoded secrets/mount paths/LAN-only assumptions in frontend source or built assets; **public URL hosting and Google/OIDC IdP not activated** — require a separate security OpenSpec change before internet exposure. **US-040E implemented (operational usability and safety polish; Story accepted 2026-07-19; BL-015 remains open):** at-a-glance filter-scope counts (upcoming / pending / due soon 48h / deferred / blocked / failed / recently published with `published` or `linkedinApiPublished` evidence only — never `flow_a_complete` or blog handoff as published); AppShell count strip for List and Month; concise operator labels with expandable diagnostics; blocked/failed visual priority without alarm fatigue; List triage vs Month schedule comprehension preserved; clear affordance groups (view/refresh/dry-run separated from cancel); destructive cancel still behind confirmation; keyboard focus rings + Escape draft warnings; touch targets retained; dark-theme consistency across loading/empty/error/detail/confirmation/success; first screen is the operational console (no marketing landing); Vitest count/label/affordance/viewport matrix + rebuilt static assets; secrets audit clean. **US-040F implemented in console layer (modern operational UX redesign first pass; Story accepted 2026-07-19; BL-015 remains open; further operator UX iteration expected):** OpenSpec change `redesign-flow-a-linkedin-variant-supervision-console-us-040f` restores continuity for a wide dark app layout with top app bar/session strip, interactive metric cards (reset-then-apply focus filters), compact alert stack, filter dock, card-based List triage, selected-item master-detail drawer, modernized shared ScheduleEditor drawer, cleaner Month calendar heading/agenda, local schedule labeling on List cards, reduced visible endpoint/policy prose, responsive mobile card/calendar behavior, and Vitest US-040F evidence plus rebuilt static assets while preserving US-040A–E contracts. Browser screenshot matrix incomplete; deploy held until UX direction is closer to operator expectation. **US-040G implemented and deployed to `192.168.0.194:8010` (calendar-first Week + Month; List removed from operator chrome; Story accepted 2026-07-19; BL-015 remains open):** OpenSpec change `redesign-flow-a-linkedin-variant-supervision-console-us-040g` (archived; git `bea6106`) — `ConsoleView` is `week` | `month` (Week default); day-column Week layout (not hour grid) with today emphasis, prev/next week, Today/This week; Month remains secondary density view; metric chips filter + navigate Week/Month only (never List); interim event-chip → ItemDetail/ScheduleEditor path (**superseded by EventModal in US-040H**; InterimEventPanel retired); List production path deleted. **Empty-grid follow-up applied in console source (OpenSpec change `fix-us-040g-outlook-empty-calendar-grid`; not yet redeployed):** empty Week/Month keep `week-columns` / `calendar-grid` visible with a calm empty cue (Outlook-like blank days); clear-filters when filters hid everything; Vitest asserts empty-state testids coexist with calendar structure; rebuilt static assets `index-SFvEuRPX.js` / `index-DTJ5Tm4v.css`; **redeployed 2026-07-18** to `192.168.0.194:8010` (git `4ebf3b8`; live HTML serves those assets; verify OVERALL PASS). Secrets audit clean on touched paths. **Empty-grid fix deployed; Visual DoD / operator walkthrough / Story accepted remain open** until screenshots + walkthrough confirming Outlook-like empty weeks/months. Filters dock removal and Cancelled metric chip are **not** claimed by this fix. **US-040H implemented and deployed to `192.168.0.194:8010` (Story accepted 2026-07-19; BL-015 remains open):** OpenSpec change `redesign-flow-a-linkedin-variant-supervision-console-us-040h` — EventModal as primary event surface (supersedes InterimEventPanel / interim ItemDetail path); ephemeral toasts replace green success banners; publish-guard as app-bar chip; month-day-focus day-agenda dump removed; ScheduleEditor/edit/defer/cancel wired into modal; Vitest (incl. ~1280/~375) + `npm run build` into `src/silverman_blog_linkedin/static/linkedin-variant-supervision-console/` (assets e.g. `index-P0TKf1cr.js` / `index-BGvbD0Jm.css`; live HTML serves those assets after deploy git `3aa9394`). Visual DoD screenshots + operator walkthrough gated (browser capture / walkthrough not completed in apply environment). **US-040I implemented and deployed to `192.168.0.194:8010` (not Story accepted; not BL-015 closed):** OpenSpec change `redesign-flow-a-linkedin-variant-supervision-console-us-040i` — operator-local primary clock with timezone cue; local-day Week/Month bucketing (near-midnight fixtures); ScheduleEditor local-first with `*_utc` conversion only at typed API boundary; client future guard allows earlier-than-previous when still after now; routine copy stripped of “think in UTC” coaching (UTC only in expandable diagnostics); schedule-visibility month fetches padded ±1 local day via multi-month merge; Vitest (`TZ=America/Chicago`, ~1280/~375) + production build into static assets `index-DV0R4K8U.js` / `index-BGvbD0Jm.css`. Deployed live assets `index-DV0R4K8U.js` / `index-BGvbD0Jm.css` (git `a1bd3cd`). Browser Visual DoD screenshots + operator walkthrough remain open — leave Story accepted open. **US-040J implemented and deployed to `192.168.0.194:8010` (not Story accepted; not BL-015 closed):** OpenSpec change `redesign-flow-a-linkedin-variant-supervision-console-us-040j` — authenticated `POST /reopen-linkedin-variant` restores eligible `cancelled` → editable `pending` with required future `new_scheduled_at_utc` (dry-run default; idempotency; `operator_supervision` audit with `cancellation_history` / `reopen_history`; `auto_queue_eligible` true; no LinkedIn API call; fail closed for recovery cancels and ineligible states; interim defer-like duplicate-slot/saturation reused; US-040K max-2/local-day delivered in a separate change). Additive schedule-visibility fields `cancelled_at_utc` / `cancellation_phase` / `cancellation_reason` / `reopen_eligible`. Console: calm cancelled chips on Week/Month; cancelled EventModal what/why/what-next; reopen happy path (confirm → local ScheduleEditor fields → dry-run/confirm → toast → refresh); cancel remains destructive/confirmed and irreversible except via reopen. Pytest + Vitest (~1280/~375) + production static rebuild `index-BJwARkPN.js` / `index-BLiwrDxd.css`. Deployed live assets `index-BJwARkPN.js` / `index-BLiwrDxd.css` (git `ec2d000`; OpenAPI exposes `/reopen-linkedin-variant`). Browser Visual DoD screenshots + operator walkthrough remain open. **US-040K implemented and deployed to `192.168.0.194:8010` (not Story accepted; not BL-015 closed):** OpenSpec change `redesign-flow-a-linkedin-variant-supervision-console-us-040k` — shared `evaluate_local_day_density` enforces max **2 density members per operator-local day** (pending/queued/published LinkedIn + blog on schedule-visibility; cancelled/failed excluded; self-move exclusion); `operator_timezone` on defer/reopen/blog schedule-update with `SILVERMAN_OPERATOR_TIMEZONE` env fallback; fail closed (`operator_timezone_required` / `operator_timezone_invalid`) — does not silently use UTC day for K; distinct codes `linkedin_supervision_local_day_density` / `calendar_schedule_local_day_density` **additive** to interim duplicate-slot + UTC-day/72h saturation and blog max-1/UTC-day; **BL-021 MAY later supersede** this interim rule; density does not call LinkedIn API. Console: Week/Month calm full (2) / over-capacity (3+) cues; ScheduleEditor + reopen prevention; plain-language errors; EventModal fix path for grandfathered 3+ days. Pytest + Vitest (~1280/~375) + production static rebuild. Deployed live assets `index-CoeIIs9v.js` / `index-CfjReggH.css` (git `0d90b3d`; `BUILD_REVISION=0d90b3daacee…`). Browser Visual DoD screenshots + operator walkthrough remain open. **US-040M Story accepted 2026-07-19 (operator confirmed on deployed console / incognito; BL-015 remains open for US-040L):** OpenSpec change `show-completed-blog-as-published-on-console-us-040m` — `GET /flow-a/schedule-visibility` maps editorial-calendar blog `status: completed` → display `publication_state: completed` (not `planned`) with `linkedin_api_published: false`; legacy title suffix `(blog handoff completed — not LinkedIn API published)` removed; console label **Published on blog** distinct from LinkedIn **Published (API evidence)**; filters/colors include `completed`; Upcoming/Pending/due-soon helpers exclude blog `completed` as unfinished work; no calendar DB status mutation. Pytest + Vitest + production static rebuild `index-DXmpFC-X.js` / `index-CfjReggH.css` in apply; operator walkthrough closed 2026-07-19 (incognito after stale-bundle note). US-040L and Flow B untouched. US-040G–K and US-040M Story accepted remain in place (not unset by L). No BFF/DB/user-management/public hosting. **US-040L Story accepted 2026-07-19 (operator walkthrough; BL-015 closed):** OpenSpec change `redesign-flow-a-linkedin-variant-supervision-console-us-040l` — removed always-visible FOCUS/Filters dock; header **Filters** control opens `FiltersModal` hosting existing filter controls (Channel, Campaign/label, Blocked only, Due soon, publication states including `completed` / Published on blog, Reset, hidden-critical banner); calm active-filter badge when `countActiveFilters` diverges from `defaultFilters()`; metric chips remain one-click focus on shared filter state; Week/Month empty Clear filters preserved; EventModal a11y patterns (dialog/Escape/backdrop/focus trap); no new worker endpoints; no List restoration; US-040M mapping unchanged. Vitest (103 tests, ~1280/~375) + production static rebuild `index-B4ePEkH6.js` / `index-DmEqOAc6.css`. Deployed 2026-07-19 to `192.168.0.194:8010` (git `143a40b`; live `index-B4ePEkH6.js` / `index-DmEqOAc6.css`; `BUILD_REVISION=143a40bd61dc…`). Operator walkthrough closed 2026-07-19 after wider-modal polish; Story accepted; BL-015 closed. Full pytest skipped (static/console-only; no worker/Python touched). Secrets audit clean on touched frontend/static paths. See [backlog.md](product/backlog.md). Mechanics: [linkedin-variant-supervision-mechanics.md](operations/linkedin-variant-supervision-mechanics.md)
- Former BL-007 construction WIP absorbed under US-018; record: [bl-007-auto-queue-pending-handoff.md](product/bl-007-auto-queue-pending-handoff.md). **BL-007 closed 2026-07-17** after US-018/US-019/US-020 operational validation.

## Completion layers (qualified)

| Layer | Status at last baseline |
|-------|-------------------------|
| Flow A core worker pipeline | Operationally validated |
| Campaign `flow_a_complete` | Validated for test post |
| Blog handoff to public checkout | Validated |
| Git commit/push to remote (US-001) | Operationally validated (controlled smoke) |
| Live-site confirmation (US-002) | Operationally validated (controlled smoke; HTTP 200 + slug marker) |
| Site published/live | Operationally validated via US-002 probe when `live_site_confirmation` opted in; manual Git still valid when automation disabled |
| LinkedIn package/scheduling | Validated |
| LinkedIn API publication | Operationally validated (US-003 controlled smoke; `executive-recruiter` on bounded-context campaign) |
| Fully unattended Flow A | Operationally validated (BL-005 Manual + Schedule; not LinkedIn API publish) |

Do not describe any single layer as "Flow A complete" without qualification. See [GLOSSARY.md](GLOSSARY.md).

## Flow A readiness defaults

`scripts/flow_a_readiness.py` `DEFAULT_EXPECTED_COMMITS` (`88cd5bc`, `96519c3`, `9dba064`) gates validated operational capabilities via ancestry checks — not the same as **`last_verified_baseline`** (`615091c` @ `2026-07-11T07:45:00Z` above), which is a point-in-time verification snapshot. Neither is a permanent runtime requirement; use `--expected-commit` to override defaults for forks or bisect.

## Related documents

- Workflows: [flow-a-target-flow.md](workflows/flow-a-target-flow.md), [linkedin-draft-review-flow.md](workflows/linkedin-draft-review-flow.md)
- Product: [backlog.md](product/backlog.md), [bl-007-auto-queue-pending-handoff.md](product/bl-007-auto-queue-pending-handoff.md)
- ADRs: [docs/decisions/](decisions/)
- Specs: `openspec/specs/`
